{
  "id": "f0a592dd",
  "title": "Step 6: Main orchestrator (main.py)",
  "tags": [
    "implementation",
    "orchestrator"
  ],
  "status": "done",
  "created_at": "2026-03-01T08:49:57.423Z"
}

Implemented `src/minidic/main.py`:

- CLI via argparse: `--model`, `--threshold`, `--min-silence`, `--clipboard`, `--per-char`, `--device`, `-v`
- Loads ASR model on startup with progress message
- Main loop: read chunk → VAD → on START open stream session → during speech feed chunks + show draft → on END finalize + inject
- Live draft preview in terminal (overwrites line)
- Supports CGEvent injection (default), per-char mode, and clipboard fallback
- Ctrl+C graceful shutdown, cleans up stream session
- Registered as `[project.scripts] minidic = "minidic.main:main"` in pyproject.toml
- `uv run minidic --help` verified working
