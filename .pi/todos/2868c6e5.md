{
  "id": "2868c6e5",
  "title": "Step 4: ASR module (transcribe.py)",
  "tags": [
    "implementation",
    "asr"
  ],
  "status": "done",
  "created_at": "2026-03-01T08:49:57.413Z"
}

Implemented `src/minidic/transcribe.py`:

- `Transcriber` class with lazy model loading via `parakeet_mlx.from_pretrained`
- `transcribe(audio_f32)` for one-shot transcription of a complete segment
- `open_stream()` returns a `StreamSession` context manager for streaming ASR
- `StreamSession` wraps `StreamingParakeet`: `add_audio()`, `finalized_text`, `draft_text`, `full_text`
- Constants: `DEFAULT_MODEL`, `CONTEXT_SIZE=(256,256)`, `STREAM_DEPTH=1`
- Import-tested; model load deferred to runtime
